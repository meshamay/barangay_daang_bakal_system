{
  "deploy": {
    "buildCommand": "composer install --optimize-autoloader --no-scripts --no-interaction --ignore-platform-req=ext-gd --ignore-platform-req=ext-zip",
    "startCommand": "sh -lc 'mkdir -p database && touch database/database.sqlite && php artisan migrate --force && php -S 0.0.0.0:$PORT -t public'"
  }
}

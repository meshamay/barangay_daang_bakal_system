{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "sh -lc 'mkdir -p database && touch database/database.sqlite && php artisan migrate --force && php -S 0.0.0.0:$PORT -t public'"
  }
}
